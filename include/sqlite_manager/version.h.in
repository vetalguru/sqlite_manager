#ifndef SQLITE_MANAGER_VERSION_H
#define SQLITE_MANAGER_VERSION_H

// Generated from version.h.in by CMake - do not edit this file or its
// generated copy in the build tree.
//
// MAJOR.MINOR are the single source of truth in project(VERSION ...) in
// the top-level CMakeLists.txt; bump them by hand. PATCH is the number
// of commits touching src/ or include/ since the last "lib-v*" release
// tag, computed by git at configure time (see cmake/GitVersion.cmake).

namespace sqlite_manager {

// Version of the sqlite_manager library itself.
inline constexpr int kVersionMajor = @PROJECT_VERSION_MAJOR@;
inline constexpr int kVersionMinor = @PROJECT_VERSION_MINOR@;
inline constexpr int kVersionPatch = @SQLITE_MANAGER_LIB_PATCH@;

// "MAJOR.MINOR.PATCH", e.g. "0.1.4".
inline constexpr const char* kVersionString = "@SQLITE_MANAGER_LIB_VERSION@";

// Version of the bundled SQLite, queried at runtime.
// Returns a string like "3.50.4".
const char* SqliteVersion();

// Returns SQLITE_VERSION_NUMBER, e.g. 3050004.
int SqliteVersionNumber();

}  // namespace sqlite_manager

#endif  // SQLITE_MANAGER_VERSION_H
